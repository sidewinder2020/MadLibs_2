<h1> Create a New Account %>

<%= form_for(@user) do |f| %>
  <%= f.label :username %>
  <%= f.text_field :username %>

  <%= f.label :password %>
  <%= f.password_field :password %>

  <%= f.label :picture, "Add URL for a picture here - keep it RELATIVELY small" %>
  <%= f.text_field :picture %>

  <%= f.submit %>
<% end %>
